<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  BuildCraft 迁移基线专用 Checkstyle 规则集（M0.6 静态分析基线）。

  定位：基线快照，只记录现状、不上门禁（build.gradle 中 ignoreFailures = true），
  不是整改目标，刻意保持宽松：仅保留基础格式类与明显代码问题类规则，
  不含命名、缩进、Javadoc 等风格类严格约束（那类规则留给 Phase 3 之后再议）。
-->
<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="warning"/>
    <property name="fileExtensions" value="java"/>

    <!-- 允许用 @SuppressWarnings("CheckStyleRuleName") 抑制单点告警 -->
    <module name="SuppressWarningsFilter"/>

    <!-- 文件级：禁止 Tab 缩进 -->
    <module name="FileTabCharacter"/>

    <!-- 文件级：行长上限 240（宽松；imports/URL 行豁免） -->
    <module name="LineLength">
        <property name="max" value="240"/>
        <property name="ignorePattern" value="^package.*|^import.*|a href|href|http://|https://|ftp://"/>
    </module>

    <module name="TreeWalker">
        <module name="SuppressWarningsHolder"/>

        <!-- import 相关：无用/冗余 import -->
        <module name="UnusedImports"/>
        <module name="RedundantImport"/>

        <!-- 基础语句结构 -->
        <module name="NeedBraces"/>
        <module name="EmptyStatement"/>
        <module name="SimplifyBooleanExpression"/>
        <module name="EqualsHashCode"/>
        <module name="DefaultComesLast"/>
    </module>
</module>
